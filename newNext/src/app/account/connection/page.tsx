import View from './view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Account: Connection',
};

export default function EcommerceAccountConnectionPage() {
  return <View />;
}
