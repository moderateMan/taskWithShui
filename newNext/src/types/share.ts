export enum GeoScopeEnum {
  LOCAL = "LOCAL",
  REGION = "REGION",
  STATE = "STATE",
  NATIONAL = "NATIONAL",
}

export enum OpportunityStatus {
  PENDING = 'pending',
  ACTIVE = 'active',
  REJECTED = 'rejected',
  SUSPENDED = 'suspended',
}